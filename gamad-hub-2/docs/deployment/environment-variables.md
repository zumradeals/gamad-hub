# Environment Variables

Use `.env.example` as the template.

Important variables:

- `DATABASE_URL`
- `POSTGRES_DB`
- `POSTGRES_USER`
- `POSTGRES_PASSWORD`
- `JWT_SECRET`
- `API_PORT`
- `WEB_PORT`
- `NEXT_PUBLIC_API_URL`
- `ADMIN_EMAIL`
- `ADMIN_PASSWORD`

Never commit a real `.env`.
