name: Deploy Staging

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Configure SSH deployment with GitHub Secrets before enabling live staging deploy."
      - run: docker compose -f docker/docker-compose.yml config
